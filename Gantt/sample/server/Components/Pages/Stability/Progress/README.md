# Progress Folder

The `Progress` folder contains the following files:

| Filename        | Description                             |
|-----------------|-----------------------------------------|
|ProgressSample | Demonstrates a scenario where the progress value is out of range (e.g., 300). It is used to test how the Gantt Chart component manages and renders invalid progress inputs during initial load. |